# Cloudflare Pages Configuration
# For Pages, KV bindings are configured in the dashboard (Settings > Bindings)
# This file only specifies the build output directory

name = "woonuxt"

# REQUIRED: Pages build output directory
pages_build_output_dir = ".output/public"

# Compatibility settings
compatibility_date = "2024-01-01"

