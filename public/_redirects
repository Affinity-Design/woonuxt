# Cloudflare Pages Redirects Configuration
# CRITICAL: This file controls how Cloudflare Pages handles routing
#
# NOTE: The /* /index.html 200 SPA fallback is NOT needed for Nuxt 3.
# Nitro handles server-side routing via the Cloudflare Pages _worker.js
# Adding it causes an infinite loop warning from Cloudflare.
#
# Add specific redirects below as needed:
