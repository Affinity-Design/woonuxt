# Cloudflare Pages Redirects Configuration
# CRITICAL: This file controls how Cloudflare Pages handles routing

# SPA fallback for client-side routing
# This MUST be last - it catches all requests that don't match static files
# Cloudflare Pages automatically serves files from _nuxt/ before this rule
/* /index.html 200
